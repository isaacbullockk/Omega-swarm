{
  "name": "omega-swarm",
  "private": true,
  "version": "4.0.0",
  "type": "module",
  "scripts": {
    "start": "node server-emergency.js",
    "build": "echo 'Build already done -- assets in public/'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
